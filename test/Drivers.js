const Drivers = artifacts.require("Drivers");
const expectThrow = require("./helpers/expectThrow");

const DriverStatus ={
    Requested: 0,
    Registered: 1,
    NotRegistered: 2, 
    Failed: 3,
    Pending: 4
};

contract("Drivers", function(){
    request
});
